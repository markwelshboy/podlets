# podlets

Disposable GPU job execution for existing RunPod/Vast workers, built around `vcp` and `pod-runtime`.

Development is performed on feature branches; Phase 1 focuses on simple throwaway jobs.
